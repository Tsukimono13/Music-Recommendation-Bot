# MusiGem Telegram Bot

Telegram бот для поиска музыкальных рекомендаций на основе артистов и тэгов.

## 🚀 Деплой на Render

### Требования

- Node.js 18+ 
- Все переменные окружения из `.env.example`

### Шаги для деплоя

1. **Создайте новый Web Service на Render:**
   - Repository: подключите ваш GitHub репозиторий
   - Name: `music-bot-telegram` (или любое другое)
   - Environment: `Node`
   - Build Command: `npm install`
   - Start Command: `npm start`
   - Plan: выберите подходящий план (Free tier доступен)

2. **Настройте переменные окружения в Render:**
   - Перейдите в Settings → Environment Variables
   - Добавьте все переменные из `.env.example`:
     - `BOT_TOKEN` - токен Telegram бота
     - `ADMIN_IDS` - ID администраторов (через запятую)
     - `BACKEND_URL` - URL вашего backend API
     - `FEEDBACK_CHANNEL_ID` - ID канала для фидбэков
     - `HEALTH_PORT` (опционально) - порт для health endpoint

3. **Проверьте настройки:**
   - Health Check Path: `/health` (если используете health checks)
   - Auto-Deploy: включите для автоматического деплоя при push

### Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev
```

### Структура проекта

```
├── src/
│   ├── actions/          # Обработчики callback действий
│   ├── api/              # API клиенты
│   ├── commands/         # Команды бота
│   ├── middlewares/      # Middleware (анти-флуд, отмена и т.д.)
│   ├── scenes/           # Сцены для многошаговых диалогов
│   ├── utils/            # Утилиты
│   └── index.ts          # Точка входа
├── assets/               # Изображения (welcome.png, qr.png)
└── package.json
```

### Переменные окружения

| Переменная | Описание | Обязательная |
|-----------|----------|--------------|
| `BOT_TOKEN` | Токен Telegram бота от @BotFather | ✅ |
| `ADMIN_IDS` | ID администраторов через запятую | ✅ |
| `BACKEND_URL` | URL backend API | ✅ |
| `FEEDBACK_CHANNEL_ID` | ID канала для фидбэков | ❌ |
| `HEALTH_PORT` | Порт для health endpoint (по умолчанию 3001) | ❌ |

### Health Endpoint

Бот автоматически запускает health endpoint на порту 3001 (или из `HEALTH_PORT`).

Проверка здоровья: `GET /health`

Ответ:
```json
{
  "status": "ok",
  "timestamp": "2024-01-01T12:00:00.000Z"
}
```

### Особенности

- ✅ Анти-флуд защита (10 запросов в минуту)
- ✅ Таймауты для внешних API (30 секунд)
- ✅ Health endpoint для мониторинга
- ✅ Обработка ошибок таймаутов
- ✅ Поддержка сцен для многошаговых диалогов

### Troubleshooting

**Бот не запускается:**
- Проверьте, что все обязательные переменные окружения установлены
- Проверьте логи в Render Dashboard

**Ошибки при отправке сообщений:**
- Убедитесь, что бот добавлен в канал для фидбэков (если используется)
- Проверьте, что бот имеет права администратора в канале

**Health endpoint не отвечает:**
- Проверьте, что порт не занят другим процессом
- Проверьте логи на наличие ошибок
