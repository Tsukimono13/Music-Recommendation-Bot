# Инструкция по деплою на Render

## Быстрый старт

1. **Подключите репозиторий к Render:**
   - Зайдите на https://render.com
   - Создайте новый Web Service
   - Подключите ваш GitHub репозиторий

2. **Настройки сервиса:**
   ```
   Name: music-bot-telegram
   Environment: Node
   Region: выберите ближайший регион
   Branch: main (или ваша основная ветка)
   Root Directory: (оставьте пустым)
   Build Command: npm install
   Start Command: npm start
   ```

3. **Переменные окружения:**
   Добавьте в Environment Variables:
   - `BOT_TOKEN` - токен от @BotFather
   - `ADMIN_IDS` - ID администраторов через запятую (например: `123456789,987654321`)
   - `BACKEND_URL` - URL вашего backend API
   - `FEEDBACK_CHANNEL_ID` - ID канала для фидбэков (опционально)
   - `HEALTH_PORT` - порт для health endpoint (опционально, по умолчанию 3001)

4. **Health Check (опционально):**
   - Health Check Path: `/health`
   - Health Check Interval: 60 секунд

5. **Деплой:**
   - Нажмите "Create Web Service"
   - Render автоматически установит зависимости и запустит бота

## Важные замечания

⚠️ **Файлы assets должны быть в репозитории:**
- `assets/welcome.png`
- `assets/qr.png`

⚠️ **Данные пользователей:**
- Файл `src/data/users.json` создается автоматически
- На Render файловая система эфемерная, данные могут теряться при перезапуске
- Для постоянного хранения рассмотрите использование базы данных

⚠️ **Порты:**
- Health endpoint запускается на порту из `HEALTH_PORT` (по умолчанию 3001)
- Render автоматически назначает внешний порт через переменную `PORT`
- Health endpoint использует внутренний порт, не конфликтует с Render

## Проверка работоспособности

После деплоя проверьте:
1. Логи в Render Dashboard - должны быть сообщения "🤖 Bot started" и "🏥 Health server started"
2. Отправьте `/start` боту в Telegram
3. Проверьте health endpoint: `https://your-service.onrender.com/health`

## Troubleshooting

**Ошибка "BOT_TOKEN is not defined":**
- Проверьте, что переменная `BOT_TOKEN` добавлена в Environment Variables
- Убедитесь, что значение сохранено (без пробелов в начале/конце)

**Ошибка "BACKEND_URL is not set":**
- Добавьте переменную `BACKEND_URL` в Environment Variables
- Убедитесь, что URL правильный (с http:// или https://)

**Бот не отвечает:**
- Проверьте логи в Render Dashboard
- Убедитесь, что бот запущен (статус "Live")
- Проверьте, что токен бота правильный

**Health endpoint не работает:**
- Health endpoint работает на внутреннем порту, не доступен извне по умолчанию
- Для доступа извне настройте Health Check в Render или используйте другой подход
